# Skill Patch

## [2026-09-12] v1.0.6

- 修复 21–79 字符内容仍落在约 190/220px 文本列中频繁纵向换行的问题，并让此前绕过分类的来源附录复用同一列级审核。
- 新增统一可见宽度算法：中文及 Unicode 全角字符计 1 个视觉单位，半角字符计 0.5，组合符与控制字符计 0；扫描表头与整列全部已格式化可见单元格，隐藏来源说明不参与计宽。
- 整列最大值 `≤10` 保持紧凑；`>10` 按 `min(48, max(16, 4 × ceil((最大视觉单位 + 2) / 4)))em` 分档加宽；超过 48em 容量后在封顶列内换行，原文不截断、不省略、不隐藏。
- 统一覆盖业务数据表、图表数据摘要、雷达对比表、关键词表和来源附录；5 列、4 列含加宽列及 14 列布局继续执行宽表/密集表规则。屏幕端滚动只在键盘可达表格容器内，页面根级禁止横向溢出；打印端清除动态宽度并恢复换行。
- 模板版本由 `1.0.4` 升至 `1.0.5`，Skill 版本由 `1.0.5` 升至 `1.0.6`；`amazon-html-report/v1`、JSON Schema、CLI 参数和回执字段未变。历史报告与回执保持只读，版本或 SHA 不匹配仍由消费方标记 `STALE`。
- 新增失败模式与回归：只看首行/单元格而漏掉整列最大值、使用字符数误判 CJK/半角混排、10/10.5/11 和 20/21 边界漂移、超过 48em 后截断、来源附录绕过审核、动态最小列宽污染打印、表格将页面根级撑宽。
- 验证完成：Renderer 40/40 单元与集成测试、6/6 Golden、20/20 Trigger eval、同步 dry-run、AmazonContext 与 DataPipelineContract 均通过；四布局 40/40 Chrome 场景通过，控制台异常、页面异常和外部请求均为 0，长文本原文完整。3 列与 4 列性能样本最大行高下降 25.45%、页面长度下降 11.25%；直接消费方 46/46、Listing 子报告签收 1/1 通过。D05 与 D12 均为 `PASS`，正式评分器为 `READY/PASS`；Renderer 本体结合全局 description 总预算与单文件 2458 行两项非阻断告警，综合发布结论为 `PASS_WITH_LIMITS`。消费方轻量体检另记录一项既有兼容告警：两个周报副本的 frontmatter `version` 不被通用 quick validator 接受，但触发、路径与 renderer 契约测试均已通过。

## [2026-09-07] v1.0.5

- 新增 `scripts/sync_to_codex.py`：Codex 安装目录与项目副本的一键同步入口，支持 `--dry-run` 预览、`--prune` 清理、逐文件 SHA-256 校验与 `--json` 输出。发现本项目 `skill-work/` 本身就是指向 `~/.codex/skills/amazon-html-report-renderer/` 的 junction（`~/.codex` 又 junction 到 `D:\DevData\.codex`），因此编辑项目即编辑已安装 Skill；脚本检测源与目标为同一物理目录时报告 `in-sync via junction` 并退出，独立副本（如 git 检出）场景才执行复制与哈希校验。
- SKILL.md 新增「Codex 部署与同步」章节：安装位置（junction 机制）、同步命令、变更生效（重启 Codex）、`config.toml` 禁用方式与 `allow_implicit_invocation` 触发策略；description 维持原 66 字首句倒金字塔写法（首句 ≤80 字符、0 触发 issues），不改动已通过审计的触发契约。
- 修正验收口径：单元与集成测试 33/33（此前文档沿用 28/28 旧值）；回归清单新增 `sync_to_codex.py --dry-run`（junction 场景报告 in-sync，独立副本场景显示 0 待变更）。
- 重新生成 `evals/optimizer-audit.json` 至 v1.0.5，更新审计日期与全部证据哈希。
- 不涉及渲染行为、协议、Schema、CLI 或回执字段变更；`report_renderer.py` 2371 行规模超建议值属既有维护性 WARN，本轮不拆分（与历史审计决定一致）。

## [2026-09-05] v1.0.4

- 真实 VOC runner 将 staging 目录搬到最终目录后，旧绝对 receipt 路径悬空，虽 HTML 字节哈希正确仍妨碍定位。
- 新增 opt-in `--receipt-path-mode relative`，默认 absolute 不变；相对路径以 receipt 所在目录为基准，路径模式与字段纳入 manifest。stdout 继续返回本次物理绝对路径，不改渲染版式、业务状态或覆盖门。
- 新增默认绝对路径兼容与相对产物对搬迁后不改收据的回归。历史报告/收据只读，不自动迁移。回滚使用本次 implementation 备份。

## [2026-09-03] v1.0.3

- 内容密集表格：5 列以上，或 4 列且含长文本证据时自动进入宽表布局；横向滚动限定在表格容器，页面本身不得被撑宽。
- 前 4 个优先字段中，不超过 20 个字符的表头和单元格保持完整单行；长证据列保留 42–72rem 的阅读宽度并正常换行。
- 所有短内容列取消统一固定宽度，改为按表头与最长内容自动收紧、强制单行，并通过现有左右内边距保留少量留白；覆盖整数、小数、百分比、布尔值、短布尔文本、日期、普通短文本以及序号、编号、排名等短标识列。
- 宽表从固定布局改为自动列宽，避免短字段 `nowrap` 在固定列宽下溢出覆盖相邻单元格；新增内容密集 5 列回归。
- 参考旧版 VOC 报告保留稳定滚动条与严格中文断行，但不复用其按具体表名写死的列宽和旧 HTML 生成链路。

## [2026-08-23] v1.0.1

- 为 COMPLETE、PARTIAL、BLOCKED 与 DEMO 报告统一增加“回到顶部”导航契约。
- 基础能力固定为原生 `<a href="#report-top">`，保证无 JavaScript 可用；JavaScript 仅通过 `IntersectionObserver` 渐进显隐，不接管锚点导航。
- 增加打印隐藏、44×44 触控目标、正常 Tab 顺序、可见焦点、移动端安全区域和 `prefers-reduced-motion` 约束。
- Golden 1–6 全部增加控件验收，避免只在某个布局族或正常报告中验证。
- 视觉 QA 等待目标文档提交后再检查，分别记录 layout/visual viewport；宽表被限制在自身横向滚动容器内，不能把 390×844 页面或固定控件推出可视区。
- 新增失败模式：缺失/重复 `#report-top`、使用 button 或脚本滚动替代原生锚点、无 JS 时控件不可用、`scroll`/定时器轮询、键盘不可达或焦点不可见、reduced-motion 仍强制动画、固定控件遮挡业务内容、BLOCKED 页漏控件、打印仍显示控件。
- 实现与文档已同步；28/28 单元与集成测试、6/6 Golden、40/40 Chrome 场景、496/496 视觉断言及 D05+D12 审计均已通过。

## [2026-08-23] v1.0.0

- 创建 `amazon-html-report/v1` 严格 JSON 协议、四个布局族、13 类组件和确定性离线渲染器。
- 增加 CSP、HTML 转义、图片格式/大小/路径、凭证/PII、来源引用、派生收据与未知组件阻断。
- 增加双产物暂存、备份恢复、哈希读回、默认拒绝覆盖和单 JSON stdout。
- 固化 Taste 5/2/7、单一 Amazon 橙、主题锁、打印浅色、CJK 与可访问性规则。
- 建立六组 Golden、20 条 Trigger eval、24 项单元/集成回归和四布局 40 场景 Chrome 视觉 QA。
- 修复 CSP 固定策略单引号被实体化导致的静态审计失败，并将 BLOCKED 字样持续呈现在数据质量页。
- 优化器兼容层只映射既有 Golden runner，不复制业务断言或渲染逻辑。
## [2026-08-24] v1.0.2

- 宽表布局：按 7–13 列与 14 列以上分级；关键词/ASIN 主列、文本参数列、数值列分别分配固定宽度和换行规则，宽表仅在自身容器横向滚动，避免把数值与单位压成多行。
- 回归：新增 7 列宽表列角色断言；保留打印媒体下的自适应布局，避免超宽表扩展打印页。
